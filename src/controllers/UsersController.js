export class UsersController {
  /**
   * index - GET to list many records.
   * show - GET to display a specific record.
   * create - POST to create a record.
   * update - PUT to update a record.
   * delete - DELETE to remove a record.
   */
}
